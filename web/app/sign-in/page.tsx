// Phase 2 scaffold — placeholder only.
// Phase 3 wires real auth: Google OAuth, Magic Link, SSO (OIDC + SAML).
export default function SignInPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-slate-400">Sign in — Phase 3</p>
    </main>
  )
}

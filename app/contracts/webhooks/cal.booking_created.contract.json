{
  "contract": {
    "authority": ["R2 Canonical"],
    "governs": "Cal.com Webhook — BOOKING_CREATED",
    "path": "app/contracts/webhooks/cal.booking_created.contract.json",
    "source": "Tax Monitor Pro",
    "title": "Tax Monitor Pro — Cal.com Webhook Contract — BOOKING_CREATED",
    "validation": {
      "enumStrict": true,
      "normalizeCheckboxToBoolean": true,
      "rejectUnknownValues": true
    },
    "version": 1
  },
  "event": {
    "required": ["id", "type", "created"],
    "properties": {
      "id": { "type": "string" },
      "type": { "type": "string", "enum": ["BOOKING_CREATED"] },
      "created": { "type": "number" }
    }
  },
  "data": {
    "type": "object",
    "description": "Vendor payload pass-through. Worker must store receipt and map to support canonical."
  }
}